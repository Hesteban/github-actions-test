name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Check environment
          run: |
            echo $GITHUB_SHA
            echo $GITHUB_REF
